using UnityEngine;
using System.Collections;

public class TransMouseAttach : MonoBehaviour {
    /*public Camera focusCamera;
    
	// Update is called once per frame
	void FixedUpdate () {
        if(focusCamera != null) {
            transform.position = focusCamera.ScreenToWorldPoint(Input.mousePosition);
            //transform.position
        }
	}*/
}
